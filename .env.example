# GymTracker Admin — Variáveis de Ambiente

# URL base da API FastAPI (apenas servidor — não exposta ao cliente)
API_URL=http://localhost:8001/api/v1

# Chave pública RSA para criptografia de senha (exposta ao cliente)
NEXT_PUBLIC_ENCRYPTION_PUBLIC_KEY=

# Chave privada RSA para descriptografia de senha (apenas servidor)
ENCRYPTION_PRIVATE_KEY=
